// import { formatMessage } from '../../../../../TextProvider';

export const validate = () => {
  // const arrKey = Object.keys(values);
  // const errors = {};
  // arrKey.forEach((key) => {
  //   if ((typeof values[key] === 'string' && values[key] !== '' && key.indexOf('rq_note') !== -1)) {
  //     if ((Array.isArray(values[`rq_Tipo_${key.slice(8)}`]) && values[`rq_Tipo_${key.slice(8)}`].length === 0)) {
  //       errors[`rq_Tipo_${key.slice(8)}`] = formatMessage('err', key.slice(3), 'require').split('_').join(' ');
  //     }
  //   }
  //   if ((Array.isArray(values[key]) && values[key].length > 0 && key.indexOf('rq_Tipo') !== -1)) {
  //     if ((typeof values[`rq_note_${key.slice(8)}`] === 'string' && values[`rq_note_${key.slice(8)}`] === '')) {
  //       errors[`rq_note_${key.slice(8)}`] = formatMessage('err', key.slice(3), 'require').split('_').join(' ');
  //     }
  //   }
  // });
  // if (Object.keys(errors).length > 0) {
  //   return errors;
  // }
  // return errors;
};

export default validate;
